using UnityEngine;
using System.Collections;

public class DJ_UIManager : MonoBehaviour {

    public static GameObject pauseButton;
    public static GameObject pauseOverlay;

	// Use this for initialization
	void Start () {
        //pauseButton = GameObject.FindGameObjectWithTag(DJ_Tag.DJ_PauseButton.ToString());
        //pauseOverlay = GameObject.FindGameObjectWithTag(DJ_Tag.DJ_PauseOverlay.ToString());

        //NGUITools.SetActive(DJ_UIManager.pauseOverlay, false);
        //NGUITools.SetActive(DJ_UIManager.pauseButton, true);
	}
	
	// Update is called once per frame
	void Update () {
	}
}
